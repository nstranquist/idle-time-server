const newTask = require('./new-task.json');
const createdTask = require('./created-task.json');
const tasks = require('./tasks.json');
const badTaskData = require('./bad-task-data.json');

module.exports = {
  newTask,
  createdTask,
  tasks,
  badTaskData,
};
