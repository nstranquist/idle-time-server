const newUser = require('./new-user.json');
const createdUser = require('./created-user.json');
const badUserData = require('./bad-user-data.json');

module.exports = {
  newUser,
  createdUser,
  badUserData,
};
