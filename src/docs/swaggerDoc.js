const swaggerJsDoc = require('swagger-jsdoc');

const swaggerOptions = {
  swaggerDefinition: {
    // openapi: '3.0.0', // defaults to swagger '2.0'
    info: {
      title: 'IdleTime API',
      version: '1.0.0',
      description: 'Node/Express API with autogenerated swagger docs',
      // contact: { name: "IdleTime Support", email: "", url: "", },
    },
    host: 'http://localhost:8080',
    // basePath: '/v1'
  },
  apis: [ './routes/index.js' ]
};

const swaggerSpec = swaggerJsDoc(swaggerOptions);

module.exports = swaggerSpec;
